import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const proxSidebar: SidebarsConfig = {
  docs: [
    {
      type: 'autogenerated',
      dirName: '.' // Usa '.' para generar sidebar desde la estructura de directorios actual
    }
  ]
};

export default proxSidebar;